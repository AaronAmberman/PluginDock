namespace PluginDock.Modeling
{
    /// <summary>Specifies the various values for instance allowance.</summary>
    public enum InstanceAllowance
    {
        /// <summary>Allows multiple instances.</summary>
        Multiple = 0,

        /// <summary>Allows only one instance.</summary>
        Single = 1
    }
}